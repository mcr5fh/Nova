# Anthropic API key (required)
ANTHROPIC_API_KEY=your-api-key-here

# OpenAI API key (optional, for TTS and Whisper transcription)
# If set, enables:
#   - /api/tts endpoint for server-proxied TTS requests
#   - /api/transcribe endpoint for Whisper speech-to-text
# This keeps the API key secure on the server instead of exposing it in the browser
# OPENAI_API_KEY=your-openai-key-here

# Server port (optional, default: 3001)
# NOVA_PORT=3001

# Server host (optional, default: 0.0.0.0)
# NOVA_HOST=0.0.0.0

# Model ID (optional, default: claude-sonnet-4-20250514)
# NOVA_MODEL=claude-sonnet-4-20250514

# CORS origin for frontend requests (optional, default: *)
# Set to your frontend URL in production for security
# NOVA_CORS_ORIGIN=http://localhost:5173
