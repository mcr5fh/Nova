# Nova Web Environment Variables

# WebSocket server URL (optional, default: ws://localhost:3001/ws)
# VITE_WS_URL=ws://localhost:3001/ws

# TTS Configuration (choose one method)

# Option 1 (Recommended): Server proxy URL for TTS
# Keeps OpenAI API key secure on the server
VITE_TTS_PROXY_URL=http://localhost:3001/api/tts

# Option 2: Direct OpenAI API calls (not recommended for production)
# Exposes API key in browser - only use for development
# VITE_OPENAI_API_KEY=your-openai-key-here

# Speech Recognition Configuration

# API URL for server proxy endpoints (transcription, etc.)
# Default: http://localhost:3001
# VITE_API_URL=http://localhost:3001

# Use Whisper transcription instead of Web Speech API (default: true)
# Set to 'false' to use browser's Web Speech API instead
# VITE_USE_WHISPER=true
