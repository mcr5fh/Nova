# OpenAI API Key (required for BAML)
OPENAI_API_KEY=sk-...

# Server Configuration
# Agent Loop Server (WebSocket chat interface)
AGENT_LOOP_HOST=0.0.0.0
AGENT_LOOP_PORT=8001

# Dashboard Server (REST API for status/tasks)
# Uses port 8000 by default (see program_nova/dashboard/server.py)

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:5173

# Agent Configuration
MAX_AGENT_ITERATIONS=10
AGENT_TIMEOUT_SECONDS=300
